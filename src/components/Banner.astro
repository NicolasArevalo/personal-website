---
/* const closeBanner = () => {
    const banner = document.querySelector('.banner');
    banner.classList.add('hide');
} */
function closeBanner() {
    const banner = document.querySelector('.banner');
    banner.classList.add('hide');
}
---

<div class="banner">
    <small>Hay nuevo capitulo del podcast :)</small>
    <a href="https://twitch.tv/nicolas_argoz">ver aquí</a>

   <!--  <div class="xclose">
        <button onclick={()=> document.querySelector('.banner').classList.add('hide') }>x</button>
    </div> -->
</div>

<style>
    .banner {
        width: 100vw;
        height: 1.5em;
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: row;
        background-color: var(--theme-accent);
        gap: .6em;
    }

    .banner small { 
        color: var(--color-white);
        text-align: center;
    }
    .banner a{
        color: var(--color-white);
        font-size: 0.833rem;
        text-decoration: underline;
    }
    .banner .xclose {
        position: absolute;
        right: 2em;
        top: 0;
    }
    .hide{
        display: none;
    }
</style>