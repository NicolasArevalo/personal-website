---
/* const closeBanner = () => {
    const banner = document.querySelector('.banner');
    banner.classList.add('hide');
} */
function closeBanner() {
    const banner = document.querySelector('.banner');
    banner.classList.add('hide');
}
---


<div class="banner">
    <small>Hay nuevo capitulo del podcast :)</small>
    <a href="https://anchor.fm/nicols-arvalo/episodes/El-tnel--cap-V---Ernesto-Sabato-e1lkoqo" target="_blank">ver aquí</a>

    <div class="xclose">
        <span id="closeBtn">
            <svg xmlns="http://www.w3.org/2000/svg" class="logo" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <title>Cerrar</title>
                <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
        </span>
    </div>
</div>
    <script is:inline>
    
        const banner = document.querySelector('.banner')
        const btn = document.querySelector('.xclose')
        
        btn.addEventListener('click', () => banner.classList.add('hide') )

    </script>

<style>
    .banner {
        width: 100vw;
        height: 2.2rem;
        position: relative;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: row;
        background: rgba(var(--color-purple-rgb), .9);
        gap: .6em;
    }
    .hide{
        display: none;
    }
    .banner small { 
        color: var(--color-white);
        text-align: center;
    }
    .banner a{
        color: var(--color-white);
        font-size: 0.833rem;
        text-decoration: underline;
    }
    .banner .xclose {
        position: absolute;
        right: 2em;
        top: 0;

    }
    .xclose{
        cursor: pointer;
    }
    svg{
        color: var(--color-white);
        margin-top: 7px;
    }
</style>