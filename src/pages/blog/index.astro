---
import LayoutLink from '@layouts/LayoutLink.astro'
import links from '@components/links'

// Component Script:
let title = 'Blog';
let description = 'Hola! Soy Nicolás. Soy ingeniero de software y desarrollador web, aunque me gustan muchas más cosas que puedes conocer en un solo lugar: aquí.';

// Data Fetching: List all Markdown posts in the repo.
/* const mdPosts = await Astro.glob('./*.md');
 */const mdxPosts = await Astro.glob('./*.md');
let allPosts = [...mdxPosts, /* ...mdPosts */];
allPosts = allPosts.sort((a, b) => new Date(b.frontmatter.publishDate).valueOf() - new Date(a.frontmatter.publishDate).valueOf());

---
<LayoutLink title='Blog de Nico' nombre='Blog' link={links.blog}>
	<div>
        
		<a href={links.blog} class="flex justify-center align-center"> <svg id="Capa_1" data-name="Capa 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 280 280">
			<title>Nicolás Arévalo Logo</title>
			<polygon points="92 277 117.5 78.5 163 141.5 186.5 3.5 182 216.5 132 153 92 277" />
		</svg> Blog</a>
    </div>
</LayoutLink>

<style>
    div{
        display: inline-block;
        transform: scale(2);
    }
</style>

